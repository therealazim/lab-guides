services:
  - type: web
    name: kmi-lupic-lab
    env: node
    buildCommand: pnpm install --no-frozen-lockfile && pnpm build
    startCommand: node server/index.js
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: NODE_VERSION
        value: 20
